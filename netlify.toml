# netlify.toml
# Instrucciones para Netlify: cómo construir la web y dónde está cada cosa.

[build]
  functions = "netlify/functions"
  publish = "dist"

[dev]
  command = "vite"
  port = 8888
  targetPort = 5173
  framework = "vite"